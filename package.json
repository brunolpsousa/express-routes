{
  "name": "routes-array",
  "version": "0.2.0",
  "description": "Back-end using arrays as DB",
  "main": "dist/app.js",
  "scripts": {
    "start": "nodemon -e ts --exec 'npm run build'",
    "build": "tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^3.0.1",
    "typescript": "^5.1.6"
  }
}
